# Generated / local-only data merges
data/legacy-knowledge.json
data/knowledge-bundle.json

# Local tariff snapshots (use scripts/tariff-snapshot.mjs)
data/versions/*.json
!data/versions/index.json
!data/versions/tax-v2026-01-01-base.json

# Runtime access telemetry (ephemeral on Vercel)
data/access-log.jsonl
data/ml-log.jsonl

# Raw benchmark reports chứa mô tả tờ khai khách (PRIVATE). Chỉ commit bản tổng hợp
# public-safe: data/accuracy-latest.json (không có productName).
data/accuracy-report-*.json

# macOS / editor junk
.DS_Store
**/.DS_Store
.vscode/
.codegraph/

# Env
.env
.env.*
!.env.example

# Oz internal declarations — PRIVATE, do not commit
data/oz-export/*.xlsx
data/oz-export/*.csv
data/oz-export/*.jsonl
data/oz-declarations.jsonl
.vercel

# Oz private derived data (raw + trung gian — KHÔNG commit)
data/oz-*.jsonl
data/oz-*.json
data/oz-declaration-embeddings.json

# EXCEPTION: gold dataset đã làm sạch — COMMIT để production dùng.
# CEO duyệt 2026-05-29: privacy không critical, cho lên git + Vercel.
# (raw xlsx + file trung gian oz-declarations/extracted/distinct vẫn ignored)
!data/oz-gold-final.jsonl

# Phương pháp HS proprietary của CEO — KHÔNG commit lên public repo
docs/methodology/

# Loại khác per-chapter build artifacts (regenerate với build-loai-khac-chapter.mjs)
data/loai-khac/
